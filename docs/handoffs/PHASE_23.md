# Phase 23 Family A RTDSM current-use-rights review handoff

## Objective and exclusions

Freeze one deterministic, portable, database-free, network-disabled technical review of current
public terms for the Phase 22 Philadelphia Fed RTDSM candidate. The exact truthful result is:

```text
outcome:              BLOCKED
review_state:         PUBLIC_TERMS_RIGHTS_REVIEW_FROZEN
aggregate_conclusion: BLOCKED_PUBLIC_TERMS_INSUFFICIENT_FOR_PERSISTENT_AUTOMATED_MODEL_USE
```

This phase completes only the review output for
`INDEPENDENT_CURRENT_USE_RIGHTS_AND_REVOCATION`; that requirement remains unsatisfied. Do not treat
the artifact as legal advice, a rights grant, entitlement, product selection, data qualification,
operational composition, research eligibility, risk approval, or execution authority.

## Accepted input identity

```text
accepted Phase 22 commit: 1c07fbe8e23950e8c9f910b30473c900c0bf3e21
accepted Phase 22 tree:   1261f5a9da883e14a894b33e583068681f8cf459
Phase 22 merge commit:    7f3bf3df029a894660f0e47dda1056bd32dca297
Phase 22 artifact id:     9d763c2d-af50-5403-9646-50a88c962bd7
Phase 22 artifact SHA:    6f6079b69838cdd292f3d426c0b1e23deeec35eaeed9f4129aa129585913abe1
Phase 22 policy SHA:      dbd7f77b646d3386d17e889cd81a3a25aed099bfc3451c37844d02d87404ba5f
```

The merge commit must remain an ancestor and must retain the accepted Phase 22 tree. Preserve the
Phase 22 artifact and domain implementation byte-for-byte.

## Exact write allowlist

Write only the Phase 23 domain, artifact, decisions/handoff, generator/verifier, focused and static
tests, phase-aware verification/CI wrappers, inherited browser phase selectors, PostgreSQL head
mapping, repository status documentation, and historical static assertions that must recognize the
new active phase. No API, migration, schema, contract, dependency, Compose, runtime, or product
behavior file is allowed to change.

## Required implementation

- Strict frozen models for three official source citations, one RTDSM rights finding, four ordered
  future-requirement statuses, and the aggregate artifact.
- Pure deterministic construction with separate row, manifest, policy, and artifact hash domains.
- A stdout-only generator requiring `--confirm-public-terms-rights-review-only`.
- A bounded offline verifier requiring `--review PATH`, with canonical byte parity and sanitized
  failure behavior.
- Static and full Phase 23 acceptance that inherits the Phase 22 gate, proves zero schema/data
  writes, and executes the new portable and network-denial checks.

Artifact output must be written only to
`docs/PHASE_23_FAMILY_A_RTDSM_CURRENT_USE_RIGHTS_REVIEW.json`. The public web pages are evidence for
the human technical assessment only; runtime generation and verification must not browse, capture
remote bodies, or depend on current network state.

## Acceptance

Run focused Phase 23 unit/portable/static tests, repository lint/format/type checks, and
`python scripts/verify_phase1.py --static-only --phase 23`. After an intentional commit, run the
complete Windows Phase 23 Compose gate, then require Ubuntu `preflight`, `unit`, and
`phase23-compose` at the exact same commit and tree. Keep all provider credential environment
variables empty and prove cleanup.

## Stop condition

Stop after Phase 23 implementation and same-SHA Windows/Ubuntu acceptance. Do not begin Phase 24;
contact a provider or counsel; use a login, credential, entitlement, account, or subscription;
request, download, inspect, or persist provider data; perform fitness or BLS reconciliation; fill an
operational composition; define a policy/holdout; execute research; compute or claim performance;
promote a strategy; mutate risk/governance; submit or reconcile an order; or add a live path without
separate authorization.
